# About the User

<!-- Durable facts about the user: role, tech stack, working style, preferences. -->
<!-- This file is always read at session start, so keep it concise -- a handful -->
<!-- of high-signal lines, not a running log. Add entries yourself or let Claude -->
<!-- add them as it learns things worth keeping. -->

- Prefers to be addressed in Spanish.
- Works on this repo as Arduino practice sketches (student/hobbyist context).
- Interested in browsing third-party GitHub repos for Claude Code "skills" and
  vetting them (checking license, whether the skill is self-contained, whether
  it depends on external tooling) before installing anything — prefers a
  manual clone-and-read approach over blindly running installer CLIs
  (`npx skills add ...`, `npx skillfish add ...`).
